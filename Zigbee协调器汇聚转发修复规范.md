# Zigbee协调器/汇聚主控转发修复规范

## 当前结论

现有 USB 协调器由厂商 Zigbee CO 模块和 CH341 串口芯片组成，资料中没有另一个可编程汇聚 MCU 工程。因此当前可执行的根本修改是：九个节点 V8.19 按 ID 错峰、仅保留最新帧，并用厂商配置工具检查协调器/节点参数。下面的“汇聚主控转发规则”只在客户后续确认另有自研协调器 MCU 并提供源码时适用，不能把旧传感器 Keil 工程当作协调器源码。

## 必须实现的转发规则

1. 每个逻辑 ID（01～09）使用独立接收状态和独立最新帧槽，禁止九路共用半包缓存、长度计数或当前设备 ID。
2. 收到一帧后先按帧头、长度和 CRC16 校验完整的 36 字节 V2 帧；只有校验通过才允许进入对应 ID 的最新帧槽。
3. PC 串口来不及发送时采用“每 ID 只保留最新一帧”的策略，覆盖该 ID 的旧待发帧；禁止 FIFO 回放数秒前的历史姿态。
4. 发送调度必须在 01～09 之间轮询公平，单个节点连续发送不得长期占用串口。建议每轮每个 ID 最多发送一帧。
5. 协调器不得修改节点的逻辑 ID、源序号、`sender_tick_ms`、硬件 ID、flags 和 CRC；应原样透传完整 V2 帧。
6. 不得以“本轮没收到某设备”为理由清空其他设备状态，也不得把上一设备的 payload 拼到下一设备 ID。
7. 禁止接收/发送路径中的长阻塞延时。串口 115200 bps 下，九路 10 Hz、每帧 36 字节约占 32.4 kbps（含起止位），带宽足够；若仍只有 1～2 Hz，应定位无线轮询或队列调度。
8. PC发出的 `A5 5A` 下行同步帧必须广播到全部九个节点，不能只送给最近活跃节点或某个固定短地址。Unity V8.20会在bit4未齐或检测到节点重启时自动重发；协调器必须允许重复同步帧通过。
9. 下行同步不得触发九节点同时ACK。节点只通过后续上行flags bit4确认，避免ACK风暴再次占满无线信道。

## 协调器必须增加的计数器

每个 ID 分别统计并可通过调试串口读取：

- 无线接收帧数；
- CRC 失败数；
- 覆盖旧待发帧数；
- 转发到 PC 的帧数；
- 最后源序号；
- 当前待发槽是否占用；
- 最大排队等待时间；
- 距最后无线帧的时间。

还要有全局串口发送字节数、串口忙次数、调度循环次数和最大单次阻塞时间。

## 验收标准

1. 单节点连续运行 2 分钟：Unity 接收 8～10 Hz，源端 Hz 约 10 Hz，CRC/乱序/重复均为 0。
2. 九节点连续运行 5 分钟：每一路 Unity 接收均不低于 8 Hz，不允许某几路长期只有其他路的约 1/9。
3. 人工暂停 PC 读取 3 秒再恢复：协调器只发送各 ID 的最新帧，不得把暂停期间旧姿态依次回放。
4. 断开任一节点：其他八路频率不得下降或重新编号。
5. Unity AI 日志中 V2 新固件应显示：
   - `source_clock_reliable=true`；
   - 正常 72 MHz 主时钟为 `source_main_clock_healthy=true`；
   - `source_backlog_age_ms` 通常接近 0；
   - `source_stale_rejected` 不持续增加。
6. Unity连接后10秒内九路 `source_link_synchronized` 应全部为true；测试中重启任一节点后，该节点应在下一轮自动重发后重新变为true，其余节点不掉线。

## 单关节坐标轴测试前置条件

只有九路各自连续 60 秒保持 8～10 Hz，并且无持续积压旧帧后，才开始单关节测试。测试顺序为 01、02、03、04、05、06、07、08、09，每次只移动对应肢段，其余保持静止，并同步提交视频与本次 Logs 文件夹。
