# 九设备通信协议 V2 与烧录说明

固件仍对应 `V8.19-ZIGBEE-SLOTTED-LATEST-20260820`；上位机从 `V8.20-PAIR-HOLD-RESYNC-20260822` 起增加同步漏收/节点重启后的自动重发。节点上行协议没有变化，因此已经分别烧录ID 1～9的V8.19固件不需要因本次Unity修复重新烧录。

## 一、每个设备必须配置不同逻辑 ID

在 Keil 中打开 `Project/Project.uvprojx`，进入：

`Options for Target -> C/C++ -> Define`

在原有宏后为每块板增加一个定义，例如：

```text
USE_STDPERIPH_DRIVER, STM32F10X_MD, DEVICE_LOGICAL_ID=1
```

九块板必须分别烧录 `DEVICE_LOGICAL_ID=1` 到 `9` 的固件。逻辑 ID 与身体部位的当前关系为：

| 逻辑 ID | 身体部位 |
|---:|---|
| 1 | 左大臂 |
| 2 | 左小臂 |
| 3 | 右大臂 |
| 4 | 右小臂 |
| 5 | 躯干 |
| 6 | 左大腿 |
| 7 | 左小腿 |
| 8 | 右大腿 |
| 9 | 右小腿 |

源码未额外指定宏时默认使用 ID 3，仅用于保持当前工程可直接编译，不能把这个默认固件烧录到九块板上。

每块 STM32 的 96 位芯片 UID 会被散列成稳定的 32 位 `hardwareId`。Unity 会锁定“逻辑 ID -> hardwareId”关系；若两块板使用了同一个逻辑 ID，后出现的板卡数据会被拒绝，并显示重复 ID 冲突，避免两套姿态交替写入同一骨骼。

## 二、V2 帧格式

总长度固定为 36 字节，多字节整数和 float 均使用小端序：

| 偏移 | 长度 | 内容 |
|---:|---:|---|
| 0 | 2 | 帧头 `AA 44` |
| 2 | 1 | 逻辑设备 ID，范围 1–9 |
| 3 | 1 | payload 长度，固定 30 |
| 4 | 16 | 四元数 `w, x, y, z`，4 个 float |
| 20 | 1 | 协议版本，固定 2 |
| 21 | 1 | 标志位，见下表 |
| 22 | 4 | 源端递增帧序号 |
| 26 | 4 | 发送端时钟，毫秒 |
| 30 | 4 | 稳定硬件 ID |
| 34 | 2 | CRC16-CCITT，低字节在前 |

CRC 参数：初值 `0xFFFF`，多项式 `0x1021`，覆盖字节 0–33。

标志位：

| 位 | 含义 |
|---:|---|
| bit0 | 身份、序号和源时钟元数据有效 |
| bit1 | 源时钟来自硬件定时器 |
| bit2 | STM32 主时钟处于预期 72 MHz 范围 |
| bit3 | 已运行“最新帧覆盖 + 按 ID 时隙发送”固件 |
| bit4 | 本次上电后已收到 Unity 的错峰同步命令 |

## 三、发送与接收约束

- 姿态传感器仍以 10 Hz 采集且只输出四元数；Zigbee 第一阶段按每路 8 Hz 上行，九路合计约 72 包/秒。
- 主控只有一个“最新四元数槽”。10 Hz 新样本直接覆盖旧样本，不建立发送 FIFO；错过本时隙的旧姿态不会在以后补发。
- 125 ms 为一个发送周期。ID 1～9 分别使用周期内不同的约 13～14 ms 时隙，禁止在时隙之外补发，从源头减少九块板同时抢占无线信道。
- 传感器二进制接收口不再进入文本命令解析器，防止随机字节误触发改频率或改输出内容。
- Zigbee USART2 下行字节先进入 64 字节中断环形缓冲区，再由主循环校验帧头、长度、命令和 CRC；不再像旧版一样接收后立即丢弃。
- 正式固件默认令 `WIRELESS_DEBUG_TEXT_ENABLED=0`。旧工程的 `printf` 同样走 USART2，九块板同时上电会争抢信道发送整段 WIT 帮助文本；V8.19 默认只允许二进制协议占用无线串口。
- Unity 按帧头、长度和 CRC16 定界；CRC 错误帧、重复帧、乱序帧和重复逻辑 ID 帧不会进入姿态队列。
- Unity 仍能读取旧版 `AA 44 + ID + LEN + payload + XOR` 帧，但旧版不能判断设备身份或源端丢帧。

## 四、Unity 下行控制帧

下行帧使用独立帧头，不改变 36 字节 V2 上行格式：

```text
A5 5A | version=01 | command | payloadLength | payload | CRC16低 | CRC16高
```

- `command=01`：同步配置，payload 为 `rateHz(1字节) + syncToken(4字节小端)`；Unity V8.20连接后立即广播，发现节点bit4未置位或源端重启时每3秒重发，全部同步后每30秒维护一次。
- `command=02`：暂停发送，payload 长度为 0；Unity 正常断开前发送。
- 节点不发送 ACK，避免九块板同时应答造成第二次碰撞。是否收到同步由上行 flags 的 bit4 逐块确认。
- 即使同步命令丢失，V8.19 节点仍默认按 8 Hz、本机时基和 ID 时隙发送；此时 bit3=1、bit4=0，日志可以准确指出未同步设备。

## 五、推荐烧录和测试顺序

1. 分别生成并烧录 ID 1–9 的九份固件，设备外壳贴上对应身体部位标签。
2. 先只上电一块板，在 Unity 遥测中确认协议版本为 2、硬件 ID 非零、源帧序号连续，并显示 `V8.20时隙 1/1、同步 1/1`。
3. 逐块增加设备；若出现“重复逻辑 ID 冲突”，停止标定并重新烧录冲突板卡。
4. 九块板全部在线后静置标定，观察源端丢帧、重复帧、乱序帧、CRC 和串口错误计数是否持续为零。
5. 拔掉任意一块板验证运行中保护：对应骨骼应保持最后一次已应用姿势，不再跳回初始姿态；数据恢复后按最大角速度平滑追赶。本次标定和九路诊断必须保留。
6. Unity V8.20 会同时显示 `接收Hz`、`源Hz`、`到达%`、`V8.20时隙`、`同步` 和腿部配对保持次数：
   - 正常目标是九路 `源Hz≈8`、`接收Hz≈8`、时隙 `9/9`、同步 `9/9`；
   - `源Hz≈8`、`接收Hz只有1~2`：节点调度正常，丢帧仍发生在无线模块、信道或接收器；
   - `源Hz也只有1~2`：优先检查对应控制板、传感器 UART、供电和是否确实烧录 V8.19；
   - 旧协议没有发送端时钟与源序号，因此 `源Hz/到达%` 显示 `--`，不能据此判断底层位置。

## 六、Zigbee 模块配置检查

源码无法自动改写无线模块的永久参数。使用资料目录中的“快速配置工具”和“网络分析台”逐块记录并核对：

1. 协调器和九个节点的 `CHANNEL`、`PANID` 必须一致，工作模式必须符合当前一对多串口透传组网。
2. 节点的角色/地址不得重复；串口统一为 `115200, 8N1`。
3. 网络分析台连续观察每个节点的入网、掉线、重连和链路质量，不要只看某一时刻显示在线。
4. 近距离桌面测试时逐级降低节点 `TX_POWER`，每次只改一个等级并保留测试日志；不是越大越稳定，九个近距离高功率节点会增加同信道竞争和接收端饱和风险。
5. 每次配置后导出或截图保存九个节点和协调器的参数，随同 Unity `Logs` 文件夹一起提交。

工具位置：

```text
D:\林兴城的文件\读研\人体姿态识别项目\硬件相关内容\ZigBee模块手册带低功耗V2\12.ZAuZx_T 系列低功耗Zigbee 无线网络串口透传解决方案快速配置工具.rar
D:\林兴城的文件\读研\人体姿态识别项目\硬件相关内容\ZigBee模块手册带低功耗V2\7.ZAuZx_T 系列低功耗Zigbee 无线网络串口透传解决方案网络分析台.rar
```

## 七、Unity V8.20 运行闸门与配对驱动

- 标定完成后先进入“标定已锁定、等待运行数据”，不会在标定完成的同一帧直接驱动人物。
- 每个参与设备必须满足运行阶段新鲜度和唯一新帧条件。V8.19 的无线目标为稳定 8 Hz，不应再靠持续放宽 Unity 掉线时间掩盖底层丢包。
- 九路同时满足后还要连续保持1秒才开始驱动，防止低频轮询数据偶然落在同一个短窗口内而误启动。
- 单路短时异常只停止该骨骼继续消费新目标，保持最后一次已应用姿势；恢复后使用最大角速度限幅追赶，不再在Rest和目标之间跳变。
- 06/07、08/09的小腿驱动只使用错位不超过200ms、年龄不超过650ms的时间配对姿态。配对失败时大腿仍可独立更新，小腿保持，不会把两个时刻的数据相减产生假屈膝。
- 串口断开或重复逻辑ID等身份不安全故障仍会停止全局驱动，但不会清除DataHub、九路Hz、帧龄、源端丢帧或标定结果。
- 手动断开、重新连接或点击重置时才会清空旧队列、硬件身份和DataHub，避免上一次连接的残留帧进入下一次标定。

## 八、单网络仍不稳定时

先以九路 8 Hz 连续测试 5 分钟。8 Hz 稳定后，才可把 Unity 的 `zigbeeScheduledTransmitRateHz` 临时改为 10 做第二阶段压力测试。若时隙和同步均为 `9/9`，但 8 Hz 仍丢包，或项目强制要求 10 Hz 而 10 Hz 不稳定，则不要继续放宽 Unity 超时门限：

1. 将设备分为 5 块和 4 块两个 Zigbee 网络；
2. 两个协调器使用互不冲突的信道和两个独立 COM 口；
3. 两组分别确认稳定后，再为 Unity 增加双串口汇聚入口。

当前 Unity 仍是单 COM 口版本，因此“两网络”属于第一阶段错峰测试失败后的下一版方案，不应在本轮尚未得到日志前提前启用。
