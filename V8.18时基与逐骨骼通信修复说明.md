# V8.18 时基与逐骨骼通信修复说明

## 本次解决的问题

- 04、06、07 的发送端毫秒时钟约慢 9 倍；
- 协调器或 Unity 队列积压后，旧帧仍可能被当作实时姿态；
- 04 右小臂超时会通过全手臂新鲜度判断牵连 01、02、03；
- 标定完成后还要等待九路同时满足运行窗口，容易长时间无法启动；
- 运行期一台设备超时会暂停九路人物驱动。

## 固件修改

1. 启动后调用 `SystemCoreClockUpdate()`，延时函数按 MCU 实际主频初始化，不再固定假设 72 MHz。
2. 使用 TIM4 建立独立硬件源端时钟；V2 的 `sender_tick_ms` 不再由 `Delayms()` 人工累加。
3. V2 flags 更新为：
   - bit0：硬件 ID、源序号和源时间有效；
   - bit1：`sender_tick_ms` 来自按实际时钟配置的硬件计时器；
   - bit2：MCU 主时钟处于预期的 72 MHz 范围。
4. 即使某块板因外部晶振/PLL 异常退回 8 MHz，10 Hz 配置延时和源端毫秒时钟仍按真实时间工作；Unity 日志会用 bit2 报出该板主时钟异常。

## Unity 修改

1. 版本号：`V8.18-SOURCE-CLOCK-INDEPENDENT-BONES-20260814`。
2. 根据可靠源端时钟与 PC 单调时钟计算 `source_backlog_age_ms`。
3. 动态积压超过 750 ms 的可靠时钟帧只计入诊断，不进入姿态历史或人物驱动。
4. AI JSONL 新增源时钟可靠性、主时钟健康、当前/最大积压年龄和旧源帧拒绝数。
5. 标定成功后直接进入逐骨骼独立驱动，不再等待九路处于同一个运行窗口。
6. 运行期仍使用约 1 秒严格帧龄保护；某一路超时只复位对应骨骼并记录 `sensor_drive_frozen`，其他骨骼继续更新。
7. 04 超时只停止右小臂输入，不再阻止左大臂、左小臂和右大臂。
8. 同侧小腿需要同侧大腿作为膝关节相对旋转参考：小腿超时不再阻止大腿更新；大腿超时时该侧大腿和相对小腿会安全复位，但不会影响另一条腿或双臂。

## 烧录要求

本次修改涉及每块节点板的真实延时、源端时钟和 V2 flags，因此 01～09 必须全部重新烧录，不能只烧录 04、06、07。

每块板分别编译并设置唯一宏：

```text
DEVICE_LOGICAL_ID=1
DEVICE_LOGICAL_ID=2
...
DEVICE_LOGICAL_ID=9
```

禁止把默认 `DEVICE_LOGICAL_ID=3` 的同一个固件烧到九块板。

## 验证顺序

1. 先逐块单机通电，确认源端 Hz 为 9～11 Hz、`source_clock_reliable=true`。
2. 如果 `source_main_clock_healthy=false`，记录对应硬件 ID；软件时基已补偿，但仍建议检查晶振和焊接。
3. 九块同时运行 5 分钟，确认每路 Unity 接收稳定 8～10 Hz，且积压拒绝数不持续增加。
4. 达到通信条件后再按 01～09 顺序进行单关节坐标轴测试。
5. 每次异常提交测试视频和同次 `Logs/日期时间/` 文件夹。

## 协调器源码状态

本机现有项目中没有 Zigbee 协调器/汇聚主控源码，不能直接完成该端代码修改。协调器必须按同目录的《Zigbee协调器汇聚转发修复规范.md》执行“每 ID 独立最新帧槽、公平轮询、禁止历史 FIFO 回放”等修改和验收。
